---
const base = import.meta.env.BASE_URL.replace(/\/?$/, '/');
const careerUrl = `${base}career`;
const projectsUrl = `${base}projects`;
---

<footer class="site-footer">
  <p>Built with curiosity, measured with data.</p>
  <div class="footer-links">
    <a href={careerUrl}>Career</a>
    <a href={projectsUrl}>Projects</a>
  </div>
</footer>
